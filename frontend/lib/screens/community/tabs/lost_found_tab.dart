import 'package:flutter/material.dart';

class LostFoundTab extends StatelessWidget {
  const LostFoundTab({super.key});
  
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Lost & Found - Coming Soon'));
  }
}